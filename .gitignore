# ──────────────────────────────────────────────────────────────
# TIER 2 — personal but portable (its own private repo)
# Clone your private state here:
#   git clone <your-private-repo> ~/.claude/private
# ──────────────────────────────────────────────────────────────
private/

# ──────────────────────────────────────────────────────────────
# TIER 3 — machine-local, never synced
# Generated by /setup on each machine.
# ──────────────────────────────────────────────────────────────
settings.json
settings.local.json
machine.md
.credentials.json

# ──────────────────────────────────────────────────────────────
# Legacy personal-state paths (pre-private/ layout).
# Kept so an older clone can't re-commit them.
# ──────────────────────────────────────────────────────────────
PROJECTS.md
ACTION-ITEMS.md
completed_archive.md
voice-profile.md
domain-profile.md
writing-samples/

decks/

# LaTeX build artifacts
*.aux
*.log
*.nav
*.snm
*.toc
*.out
*.synctex.gz
*.fls
*.fdb_latexmk

# Conversation history & session artifacts
history.jsonl
SESSION_LOG.md
sessions/
session-env/
shell-snapshots/
paste-cache/

# Machine-specific runtime state
memory/
projects/
cache/
file-history/
backups/
debug/
telemetry/
stats-cache.json
mcp-needs-auth-cache.json
.last-cleanup
.last-update-result.json
daemon/
tasks/

# Ephemeral outputs
logs/
usage-data/
staging/
plans/
todos/
downloads/

# External repos (don't nest git repos)
MixtapeTools/

# Marketplace / plugin state (auto-managed)
plugins/

# OS
.Rhistory
Thumbs.db
.DS_Store
